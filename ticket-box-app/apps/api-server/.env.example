REDIS_URL=redis://localhost:6379

# ── (Tuỳ chọn) SMTP tổng quát thay cho Gmail ─────────────────────────
# Nếu không dùng Gmail, có thể cấu hình SMTP bất kỳ:
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=
# SMTP_PASS=

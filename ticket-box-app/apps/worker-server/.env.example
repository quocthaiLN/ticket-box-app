REDIS_URL=redis://localhost:6379
NODE_ENV=development

# ── SMTP ─────────────────────────────────────────────────────────────
# Bắt buộc để email worker (OTP, ...) gửi được mail.
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
# SMTP_FROM=TicketBox <noreply@ticketbox.vn>

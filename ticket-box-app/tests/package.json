{
  "name": "@ticketbox/tests",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "generate:tokens": "node --env-file=../.env ./generate-tokens/generate-tokens.mjs",
    "verify:order-loadtest": "node ./order/verify-order-loadtest.mjs",
    "test": "vitest run --reporter=verbose",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.0.0",
    "vite": "^8.0.16",
    "vitest": "^4.1.9"
  },
  "dependencies": {
    "@ticketbox/database": "*",
    "@ticketbox/redis": "*",
    "nodemailer": "9.0.1"
  }
}


  # Frontend design for TicketBox

  This is a code bundle for Frontend design for TicketBox. The original project is available at https://www.figma.com/design/qHJeN6FOtdaiIDFCdq09xP/Frontend-design-for-TicketBox.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  